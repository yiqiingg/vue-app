describe('Filter function', () => {
  test('it should filter by a search term (link)', () => {
    expect(1 + 2).toBe(3);
  });
  test('it should filter by a search term (link)', () => {
    expect(true).toBe(true);
  });
});
